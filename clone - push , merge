create new repo....readme and open code and copy https link

now open local disk :c  and open git bash ......git clone <link> and press enter

now cloned to local repository ....create text file with some matter and open gitbash.....type   --git status u will get as untracked file

-  git add .   ,   -  git commit -m "first file added"     ,   -  git push      ,    now u will see some msg and if u open repository u will fing the tet document

now again write some change for text file and open git bash and type --git status, u will get as modified

now again  --git add ,  --git commit -m "modified the file"    , ---git push   ( previously name was first file and now it is modified the file )

then u can go to profile and check
